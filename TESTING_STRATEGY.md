# 🧪 Стратегия тестирования проекта

## 📊 Текущее состояние тестов

### Существующие тесты:
- ✅ **16 тестов** для `WooCommerceConnector`
- ✅ **3 теста** для проверки версии API
- ✅ **9 тестов** для экспорта в Excel
- ⚠️ **Проблема:** Тесты не могут импортировать `WooCommerceGUI` (нужно исправить)

### Покрытие:
- Текущее покрытие: ~55% (по старой структуре)
- Целевое покрытие: **80%+** для критичных компонентов

---

## 🎯 СТРАТЕГИЯ ТЕСТИРОВАНИЯ

### Принцип: **Test-Driven Refactoring**

**Подход:** Перед каждым рефакторингом создаем тесты для проверки работоспособности.

---

## 📋 ПЛАН ПОКРЫТИЯ ТЕСТАМИ

### 1. ТЕСТЫ ДЛЯ НОВЫХ КОМПОНЕНТОВ (Приоритет: 🔴 ВЫСОКИЙ)

#### 1.1 Модели данных (`models/`)

**Что тестировать:**
- ✅ `Product.from_dict()` - создание из API данных
- ✅ `Product.to_dict()` - преобразование в API формат
- ✅ `Product` properties (is_in_stock, has_discount, discount_percentage)
- ✅ `Category.from_dict()` и `to_dict()`
- ✅ `Order.from_dict()` и `to_dict()`
- ✅ `Customer.from_dict()` и `to_dict()`
- ✅ Edge cases (пустые данные, None значения, неполные данные)

**Файл:** `tests/test_models.py`

**Пример:**
```python
def test_product_from_dict():
    data = {
        "id": 1,
        "name": "Coffee",
        "price": "29.99",
        "regular_price": "39.99",
        "sale_price": "29.99",
        "on_sale": True,
        "stock_status": "instock"
    }
    product = Product.from_dict(data)
    assert product.id == 1
    assert product.name == "Coffee"
    assert product.has_discount == True
    assert product.discount_percentage == 25.0
```

---

#### 1.2 Конфигурация (`config/`)

**Что тестировать:**
- ✅ `WooCommerceConfig.from_env()` - загрузка из переменных окружения
- ✅ `WooCommerceConfig.validate()` - валидация конфигурации
- ✅ Ошибки при отсутствии обязательных полей
- ✅ Проверка формата URL
- ✅ Проверка формата ключей (ck_, cs_)
- ✅ Значения по умолчанию

**Файл:** `tests/test_config.py`

**Пример:**
```python
def test_config_from_env():
    with patch.dict(os.environ, {
        'WC_URL': 'https://store.com',
        'WC_CONSUMER_KEY': 'ck_test',
        'WC_CONSUMER_SECRET': 'cs_test'
    }):
        config = WooCommerceConfig.from_env()
        assert config.url == 'https://store.com'
        assert config.api_version == 'wc/v3'  # default
```

---

#### 1.3 Исключения (`api/exceptions.py`)

**Что тестировать:**
- ✅ Создание каждого типа исключения
- ✅ Сообщения об ошибках
- ✅ Наследование от базового класса
- ✅ Специфичные атрибуты (status_code, message)

**Файл:** `tests/test_exceptions.py`

**Пример:**
```python
def test_authentication_error():
    error = AuthenticationError("Invalid credentials")
    assert str(error) == "Invalid credentials"
    assert isinstance(error, WooCommerceAPIError)

def test_api_response_error():
    error = APIResponseError(404, "Not found", "Details")
    assert error.status_code == 404
    assert "404" in str(error)
```

---

#### 1.4 Логирование (`utils/logger.py`)

**Что тестировать:**
- ✅ `setup_logger()` создает logger с правильными handlers
- ✅ Логи записываются в файл
- ✅ Логи выводятся в консоль
- ✅ Уровни логирования работают правильно
- ✅ `get_logger()` возвращает существующий или создает новый

**Файл:** `tests/test_logger.py`

**Пример:**
```python
def test_setup_logger_creates_handlers():
    logger = setup_logger("test_module")
    assert len(logger.handlers) >= 1
    assert any(isinstance(h, logging.StreamHandler) for h in logger.handlers)
```

---

#### 1.5 Константы (`config/constants.py`)

**Что тестировать:**
- ✅ Все константы имеют правильные значения
- ✅ Константы доступны для импорта
- ✅ Типы данных констант корректны

**Файл:** `tests/test_constants.py`

---

### 2. ИНТЕГРАЦИОННЫЕ ТЕСТЫ (Приоритет: 🟡 СРЕДНИЙ)

#### 2.1 Интеграция моделей с API

**Что тестировать:**
- ✅ Преобразование API ответа в модели
- ✅ Преобразование моделей обратно в API формат
- ✅ Работа с реальными данными (моки)

**Файл:** `tests/test_integration_models_api.py`

---

#### 2.2 Интеграция конфигурации с connector

**Что тестировать:**
- ✅ Connector использует WooCommerceConfig
- ✅ Валидация конфигурации перед созданием connector
- ✅ Обработка ошибок конфигурации

**Файл:** `tests/test_integration_config_connector.py`

---

### 3. РЕГРЕССИОННЫЕ ТЕСТЫ (Приоритет: 🔴 ВЫСОКИЙ)

#### 3.1 Обратная совместимость

**Что тестировать:**
- ✅ Старый код все еще работает после рефакторинга
- ✅ API интерфейс не изменился
- ✅ Все существующие тесты проходят

**Файл:** Обновить существующие тесты

---

## 🏗️ СТРУКТУРА ТЕСТОВ

```
tests/
├── __init__.py
├── conftest.py              # Общие фикстуры
├── test_connector.py        # ✅ Существует
├── test_api_version_check.py  # ✅ Существует
├── test_excel_export.py     # ✅ Существует (нужно исправить)
│
├── test_models.py            # 🆕 Тесты моделей
├── test_config.py           # 🆕 Тесты конфигурации
├── test_exceptions.py        # 🆕 Тесты исключений
├── test_logger.py           # 🆕 Тесты логирования
├── test_constants.py        # 🆕 Тесты констант
│
├── test_integration_models_api.py      # 🆕 Интеграционные
├── test_integration_config_connector.py  # 🆕 Интеграционные
│
└── fixtures/                # 🆕 Тестовые данные
    ├── sample_products.json
    ├── sample_orders.json
    └── sample_customers.json
```

---

## 📝 ПРИОРИТЕТЫ ВНЕДРЕНИЯ

### Этап 1: Критичные тесты (СЕЙЧАС)
1. ✅ Исправить импорт в `test_excel_export.py`
2. 🆕 Создать `test_models.py` - тесты для Product, Category
3. 🆕 Создать `test_config.py` - тесты для WooCommerceConfig
4. 🆕 Создать `test_exceptions.py` - тесты для исключений

### Этап 2: Важные тесты (СЛЕДУЮЩИЙ)
5. 🆕 Создать `test_logger.py` - тесты для логирования
6. 🆕 Создать `test_constants.py` - тесты для констант
7. 🆕 Обновить существующие тесты для работы с новыми компонентами

### Этап 3: Интеграционные тесты (ПОСЛЕ РЕФАКТОРИНГА)
8. 🆕 Интеграционные тесты моделей с API
9. 🆕 Интеграционные тесты конфигурации с connector

---

## 🛠️ ИНСТРУМЕНТЫ И ПРАКТИКИ

### Используемые инструменты:
- ✅ **pytest** - фреймворк тестирования
- ✅ **pytest-cov** - покрытие кода
- ✅ **pytest-mock** - моки и патчи
- ✅ **unittest.mock** - стандартные моки

### Best Practices:
1. **AAA Pattern** (Arrange-Act-Assert)
2. **Один тест = одна проверка**
3. **Использование фикстур для общих данных**
4. **Моки для внешних зависимостей**
5. **Понятные имена тестов**

### Пример структуры теста:
```python
def test_product_from_dict_with_discount():
    # Arrange - подготовка данных
    api_data = {
        "id": 1,
        "name": "Coffee",
        "regular_price": "100",
        "sale_price": "75",
        "on_sale": True
    }
    
    # Act - выполнение действия
    product = Product.from_dict(api_data)
    
    # Assert - проверка результата
    assert product.has_discount == True
    assert product.discount_percentage == 25.0
```

---

## 📊 МЕТРИКИ УСПЕХА

### Целевые показатели:
- **Покрытие кода:** 80%+ для критичных компонентов
- **Скорость тестов:** Все тесты выполняются < 30 секунд
- **Стабильность:** Все тесты проходят стабильно
- **Читаемость:** Тесты понятны джуну

### Команды для проверки:
```bash
# Запуск всех тестов
pytest tests/ -v

# С покрытием
pytest tests/ -v --cov=woocommerce_connector --cov-report=term-missing

# Только новые тесты
pytest tests/test_models.py tests/test_config.py -v

# Быстрые тесты (без slow)
pytest tests/ -v -m "not slow"
```

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Создать стратегию тестирования (этот документ)
2. ⏳ Исправить импорт в `test_excel_export.py`
3. ⏳ Создать `tests/test_models.py`
4. ⏳ Создать `tests/test_config.py`
5. ⏳ Создать `tests/test_exceptions.py`
6. ⏳ Запустить все тесты и проверить покрытие

---

**Готов начать создавать тесты!** 🎯

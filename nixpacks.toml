# Railway/Nixpacks - web app без GUI (customtkinter не нужен на сервере)
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements-railway.txt"]

[start]
cmd = "uvicorn frontend.app:app --host 0.0.0.0 --port $PORT"

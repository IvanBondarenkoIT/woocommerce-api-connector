# 🗺️ Дорожная карта развития проекта

## 📌 Текущие возможности

- ✅ Подключение к WooCommerce API
- ✅ Получение товаров
- ✅ GUI для просмотра товаров
- ✅ Экспорт товаров в Excel

---

## 🎯 ПЛАНИРУЕМЫЕ ФИЧИ

### 1. РАБОТА С ЗАКАЗАМИ (Orders)

**Цель:** Получать и анализировать заказы из WooCommerce

**Задачи:**
- [ ] Добавить `models/order.py` - модель заказа
- [ ] Создать `api/orders.py` - OrdersRepository
- [ ] Создать `services/order_service.py` - OrderService
- [ ] Добавить методы:
  - `get_all_orders()` - получить все заказы
  - `get_orders_by_date_range()` - заказы за период
  - `get_orders_by_status()` - заказы по статусу
  - `get_order_by_id()` - заказ по ID
- [ ] Добавить экспорт заказов в Excel
- [ ] Добавить фильтрацию и поиск

**API Endpoints (для фронтенда):**
```
GET /api/orders - список заказов
GET /api/orders/{id} - заказ по ID
GET /api/orders?status=pending - фильтр по статусу
GET /api/orders?date_from=2024-01-01&date_to=2024-12-31 - фильтр по дате
```

---

### 2. АНАЛИЗ БАЗЫ КЛИЕНТОВ (Customers)

**Цель:** Извлекать и анализировать клиентов из заказов

**Задачи:**
- [ ] Добавить `models/customer.py` - модель клиента
- [ ] Создать `api/customers.py` - CustomersRepository
- [ ] Создать `services/customer_service.py` - CustomerService
- [ ] Добавить методы:
  - `extract_customers_from_orders()` - извлечь клиентов из заказов
  - `get_unique_customers()` - получить уникальных клиентов
  - `get_customer_statistics()` - статистика по клиентам
  - `get_customer_orders()` - заказы клиента
- [ ] Добавить анализ:
  - Количество заказов на клиента
  - Сумма покупок клиента
  - Последний заказ
  - Средний чек
- [ ] Добавить экспорт клиентов в Excel

**API Endpoints (для фронтенда):**
```
GET /api/customers - список клиентов
GET /api/customers/{id} - клиент по ID
GET /api/customers/{id}/orders - заказы клиента
GET /api/customers/statistics - статистика по клиентам
```

---

### 3. ИНТЕГРАЦИЯ С ВНЕШНИМ СЕРВИСОМ

**Цель:** Записывать новых клиентов в отдельный сервис по API

**Задачи:**
- [ ] Изучить документацию внешнего API (будет предоставлена)
- [ ] Создать `api/external_client.py` - клиент для внешнего API
- [ ] Создать `services/sync_service.py` - сервис синхронизации
- [ ] Добавить методы:
  - `sync_new_customers()` - синхронизировать новых клиентов
  - `create_customer_in_external()` - создать клиента во внешнем сервисе
  - `update_customer_in_external()` - обновить клиента
  - `check_customer_exists()` - проверить существование клиента
- [ ] Добавить валидацию данных перед отправкой
- [ ] Добавить обработку ошибок
- [ ] Добавить логирование операций
- [ ] Добавить retry механизм

**API Endpoints (для фронтенда):**
```
POST /api/sync/customers - синхронизировать клиентов
POST /api/sync/customers/{id} - синхронизировать конкретного клиента
GET /api/sync/status - статус синхронизации
```

---

### 4. ФРОНТЕНД (Web Interface)

**Цель:** Заменить GUI на веб-интерфейс

**Технологии (на выбор):**
- **Вариант 1:** Flask + Jinja2 (простой, быстро)
- **Вариант 2:** FastAPI + React/Vue (современный, масштабируемый)
- **Вариант 3:** FastAPI + простой HTML+JS (быстро, без сборки)

**Задачи:**
- [ ] Создать веб-сервер (Flask/FastAPI)
- [ ] Создать REST API endpoints
- [ ] Создать фронтенд:
  - Страница товаров
  - Страница заказов
  - Страница клиентов
  - Страница синхронизации
  - Дашборд со статистикой
- [ ] Добавить аутентификацию (если нужно)
- [ ] Добавить фильтры и поиск
- [ ] Добавить экспорт данных
- [ ] Добавить графики и визуализацию

**Структура фронтенда:**
```
frontend/
├── static/
│   ├── css/
│   ├── js/
│   └── images/
├── templates/
│   ├── base.html
│   ├── products.html
│   ├── orders.html
│   ├── customers.html
│   └── dashboard.html
└── api/
    └── routes.py
```

---

## 📊 ПРИОРИТИЗАЦИЯ ФИЧ

### Фаза 1: Рефакторинг (текущая)
- ✅ Улучшение архитектуры
- ✅ Подготовка к новым фичам

### Фаза 2: Заказы
- 🔴 Высокий приоритет
- Необходимо для анализа клиентов

### Фаза 3: Клиенты
- 🔴 Высокий приоритет
- Основная цель проекта

### Фаза 4: Интеграция
- 🟡 Средний приоритет
- Зависит от документации внешнего API

### Фаза 5: Фронтенд
- 🟡 Средний приоритет
- Улучшит UX

---

## 🔗 ЗАВИСИМОСТИ МЕЖДУ ФИЧАМИ

```
Рефакторинг
    ↓
Заказы ←──┐
    ↓     │
Клиенты ←─┘
    ↓
Интеграция
    ↓
Фронтенд
```

---

## 📝 ПРИМЕЧАНИЯ

1. **Документация внешнего API:** Будет предоставлена позже
2. **Фронтенд:** Выбор технологии зависит от требований
3. **Тестирование:** Каждая фича должна быть покрыта тестами
4. **Документация:** Обновлять README после каждой фичи

---

**Готов начать работу!** 🚀

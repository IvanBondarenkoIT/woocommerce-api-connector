# 🏗️ Архитектура LILU Connector

## 📋 Обзор

LILU Connector - это модуль для интеграции с API LILU (Servus Ululu), разработанный с использованием принципов чистой архитектуры и лучших практик Python.

## 🎯 Принципы проектирования

### 1. Separation of Concerns (Разделение ответственности)

Каждый модуль отвечает за свою область:

- **`api/`** - только HTTP запросы
- **`models/`** - только структуры данных
- **`config/`** - только конфигурация
- **`utils/`** - только вспомогательные функции
- **`connector.py`** - оркестрация всех компонентов

### 2. Single Responsibility Principle (Принцип единственной ответственности)

Каждый класс делает одну вещь:

- `LILUClient` - только HTTP запросы
- `ClientModel` - только представление клиента
- `LILUSettings` - только настройки
- `LILUConnector` - только бизнес-логика работы с API

### 3. Dependency Injection (Внедрение зависимостей)

Компоненты получают зависимости извне:

```python
# Плохо (жёсткая зависимость):
class LILUConnector:
    def __init__(self):
        self.settings = LILUSettings()  # Жёсткая зависимость

# Хорошо (внедрение зависимости):
class LILUConnector:
    def __init__(self, settings: Optional[LILUSettings] = None):
        self.settings = settings or LILUSettings()  # Гибкая зависимость
```

### 4. Error Handling (Обработка ошибок)

Используются специфичные исключения:

- `AuthenticationError` - ошибки аутентификации
- `NotFoundError` - ресурс не найден
- `NetworkError` - проблемы с сетью
- `RateLimitError` - превышен лимит запросов

## 📁 Структура модулей

### `api/` - Слой работы с API

**Ответственность:** HTTP запросы к серверу

**Компоненты:**
- `client.py` - HTTP клиент (GET, POST, PUT, DELETE)
- `exceptions.py` - специфичные исключения для API

**Принципы:**
- Инкапсуляция HTTP логики
- Обработка ошибок на уровне HTTP
- Повторные попытки при временных ошибках
- Логирование всех запросов

### `models/` - Модели данных

**Ответственность:** Представление данных из API

**Компоненты:**
- `client.py` - модель клиента
- `product.py` - модель продукта
- `order.py` - модель заказа

**Принципы:**
- Типизация данных
- Преобразование из/в словари
- Вычисляемые свойства (@property)
- Валидация данных

### `config/` - Конфигурация

**Ответственность:** Настройки и константы

**Компоненты:**
- `settings.py` - настройки из .env
- `constants.py` - константы (URL, endpoints)

**Принципы:**
- Централизация настроек
- Валидация обязательных полей
- Значения по умолчанию

### `utils/` - Утилиты

**Ответственность:** Вспомогательные функции

**Компоненты:**
- `logger.py` - настройка логирования

**Принципы:**
- Переиспользование кода
- Независимость от бизнес-логики

### `connector.py` - Главный класс

**Ответственность:** Оркестрация всех компонентов

**Принципы:**
- Удобный API для пользователей
- Преобразование данных в модели
- Обработка бизнес-логики
- Логирование операций

## 🔄 Поток данных

### Пример: Получение списка клиентов

```
1. Пользователь вызывает:
   connector.get_clients()

2. LILUConnector:
   - Формирует endpoint и параметры
   - Вызывает client.get()

3. LILUClient:
   - Строит полный URL
   - Добавляет заголовки и аутентификацию
   - Выполняет HTTP GET запрос
   - Обрабатывает ответ

4. LILUConnector:
   - Преобразует ответ в список ClientModel
   - Логирует операцию
   - Возвращает результат

5. Пользователь получает:
   List[ClientModel]
```

## 🧪 Тестирование

### Структура тестов

```
tests/
├── test_connector.py      # Тесты LILUConnector
├── test_client.py         # Тесты LILUClient
├── test_models.py         # Тесты моделей
└── test_config.py        # Тесты конфигурации
```

### Принципы тестирования

1. **Unit тесты** - тестирование отдельных компонентов
2. **Integration тесты** - тестирование взаимодействия компонентов
3. **Mock объекты** - изоляция от реального API
4. **Покрытие кода** - минимум 80% покрытия

## 📈 Расширяемость

### Добавление нового endpoint

1. Добавить endpoint в `config/constants.py`
2. Создать модель в `models/` (если нужно)
3. Добавить метод в `connector.py`
4. Написать тесты

### Добавление новой модели

1. Создать файл в `models/`
2. Использовать шаблон из `templates/model_template.py`
3. Добавить импорт в `models/__init__.py`
4. Использовать в `connector.py`

## 🔐 Безопасность

### Принципы безопасности

1. **API токен** - хранится в `.env`, не коммитится в Git
2. **Валидация** - проверка всех входных данных
3. **Обработка ошибок** - не раскрывать внутренние детали
4. **Логирование** - не логировать секретные данные

## 📚 Документация

### Структура документации

- `README.md` - основная документация
- `JUNIOR_GUIDE.md` - руководство для Junior разработчиков
- `SETUP_GUIDE.md` - руководство по настройке
- `ARCHITECTURE.md` - этот файл
- Docstrings в коде - документация функций и классов

### Принципы документации

1. **Для Junior** - объяснения простым языком
2. **Примеры** - код примеры для каждого метода
3. **Актуальность** - документация обновляется с кодом
4. **Полнота** - покрытие всех аспектов

## 🎓 Для Senior разработчиков

### Паттерны проектирования

1. **Repository Pattern** - `LILUClient` как репозиторий для API
2. **Factory Pattern** - `from_dict()` методы в моделях
3. **Strategy Pattern** - разные стратегии обработки ошибок
4. **Adapter Pattern** - адаптация API ответов к моделям

### Метрики качества

- **Cyclomatic Complexity** - низкая сложность методов
- **Code Coverage** - минимум 80%
- **Type Hints** - типизация всех функций
- **Docstrings** - документация всех публичных методов

### Рекомендации по улучшению

1. Добавить кэширование для часто запрашиваемых данных
2. Реализовать пул соединений для HTTP клиента
3. Добавить метрики и мониторинг
4. Реализовать batch операции для массовых запросов

## 📖 Связанные документы

- [README.md](README.md) - основная документация
- [JUNIOR_GUIDE.md](JUNIOR_GUIDE.md) - руководство для Junior
- [SETUP_GUIDE.md](SETUP_GUIDE.md) - настройка
- [API_NOTES.md](API_NOTES.md) - ключевые моменты из инструкции LILU API
- [Документация API LILU](../data/input/liloo_API%20v%202.0-120126-144149.pdf)

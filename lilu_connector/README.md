# LILU API Connector

## 📋 Описание

Этот модуль предоставляет интеграцию с API LILU (Servus Ululu) для работы с клиентами "Vygorsky tuda kliyentov".

## 🏗️ Архитектура (для Junior разработчиков)

### Что такое архитектура?

Архитектура - это способ организации кода, чтобы его было легко понимать, поддерживать и расширять. 
Представьте, что вы строите дом: архитектура - это план, где каждая комната имеет свою функцию.

### Структура проекта

```
lilu_connector/
├── __init__.py              # Точка входа в пакет (экспортирует основные классы)
├── connector.py             # Главный класс для работы с API
├── api/                     # Слой работы с API
│   ├── __init__.py
│   ├── client.py           # Базовый HTTP клиент
│   ├── exceptions.py       # Специальные ошибки для API
│   └── endpoints.py        # Определение всех API endpoints
├── models/                  # Модели данных (структуры данных)
│   ├── __init__.py
│   ├── client.py           # Модель клиента
│   ├── product.py          # Модель продукта
│   └── order.py            # Модель заказа
├── config/                  # Конфигурация
│   ├── __init__.py
│   ├── settings.py         # Настройки подключения
│   └── constants.py        # Константы (URL, endpoints и т.д.)
├── utils/                   # Вспомогательные функции
│   ├── __init__.py
│   ├── logger.py           # Логирование
│   └── validators.py       # Валидация данных
└── templates/               # Шаблоны для разработки
    ├── api_template.py     # Шаблон для нового API endpoint
    └── model_template.py   # Шаблон для новой модели
```

### Принципы архитектуры (SOLID)

#### 1. Single Responsibility Principle (Принцип единственной ответственности)
**Что это значит?**
Каждый класс должен делать только одну вещь и делать её хорошо.

**Пример:**
- `LILUConnector` - только подключение к API
- `LILUClient` - только HTTP запросы
- `ClientModel` - только хранение данных клиента

**Почему это важно?**
Если класс делает много вещей, его сложно тестировать и изменять.

#### 2. Separation of Concerns (Разделение ответственности)
**Что это значит?**
Разные части кода должны отвечать за разные вещи.

**Пример:**
- `api/` - только работа с HTTP запросами
- `models/` - только структуры данных
- `utils/` - только вспомогательные функции

**Почему это важно?**
Если нужно изменить способ работы с API, мы меняем только `api/`, не трогая модели.

### Как работает код (для Junior)

#### Шаг 1: Инициализация
```python
from lilu_connector import LILUConnector

# Создаём объект коннектора
connector = LILUConnector()
```

**Что происходит внутри?**
1. Читаются настройки из `.env` файла
2. Создаётся HTTP клиент (`LILUClient`)
3. Проверяется подключение к API

#### Шаг 2: Получение данных
```python
# Получаем список клиентов
clients = connector.get_clients()
```

**Что происходит внутри?**
1. `LILUConnector.get_clients()` вызывает `LILUClient.get()`
2. `LILUClient` делает HTTP GET запрос к API
3. Ответ преобразуется в объекты `ClientModel`
4. Возвращается список клиентов

#### Шаг 3: Обработка ошибок
```python
try:
    clients = connector.get_clients()
except AuthenticationError:
    print("Неверные учетные данные")
except NetworkError:
    print("Проблема с сетью")
```

## 📚 Документация API

### Основные методы

#### `LILUConnector.get_clients()`
Получить список всех клиентов.

**Возвращает:** `List[ClientModel]`

**Пример:**
```python
clients = connector.get_clients()
for client in clients:
    print(f"{client.name} - {client.email}")
```

#### `LILUConnector.get_client(client_id: int)`
Получить информацию о конкретном клиенте.

**Параметры:**
- `client_id` (int): ID клиента

**Возвращает:** `ClientModel`

**Пример:**
```python
client = connector.get_client(123)
print(client.name)
```

## 🔧 Настройка

### 1. Создайте файл `.env`

```env
# LILU API Configuration
LILU_API_URL=https://api.servus-ululu.com
LILU_API_KEY=your_api_key_here
LILU_API_SECRET=your_api_secret_here
LILU_API_VERSION=v2
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Используйте коннектор

```python
from lilu_connector import LILUConnector

connector = LILUConnector()
clients = connector.get_clients()
```

## 📖 Для Junior разработчиков: Как добавить новый метод

### Шаг 1: Определите endpoint в `config/constants.py`

```python
# В config/constants.py
ENDPOINTS = {
    'clients': '/api/v2/clients',
    'products': '/api/v2/products',
    'orders': '/api/v2/orders',
    # Добавьте новый endpoint
    'invoices': '/api/v2/invoices',
}
```

### Шаг 2: Создайте метод в `connector.py`

```python
def get_invoices(self, client_id: Optional[int] = None):
    """
    Получить список счетов.
    
    Args:
        client_id: ID клиента (опционально)
    
    Returns:
        List[InvoiceModel]: Список счетов
    """
    endpoint = ENDPOINTS['invoices']
    params = {}
    if client_id:
        params['client_id'] = client_id
    
    response = self.client.get(endpoint, params=params)
    return [InvoiceModel.from_dict(inv) for inv in response]
```

### Шаг 3: Создайте модель в `models/invoice.py` (если нужно)

```python
@dataclass
class InvoiceModel:
    id: int
    client_id: int
    amount: float
    status: str
    
    @classmethod
    def from_dict(cls, data: dict) -> 'InvoiceModel':
        return cls(
            id=data['id'],
            client_id=data['client_id'],
            amount=data['amount'],
            status=data['status']
        )
```

## 🧪 Тестирование

### Запуск тестов

```bash
pytest tests/test_lilu_connector.py
```

### Пример теста

```python
def test_get_clients():
    connector = LILUConnector()
    clients = connector.get_clients()
    assert len(clients) > 0
    assert isinstance(clients[0], ClientModel)
```

## 📝 Шаблоны

В папке `templates/` находятся шаблоны для:
- Создания нового API endpoint
- Создания новой модели данных
- Создания нового метода коннектора

Используйте их как основу для нового кода.

## ❓ Часто задаваемые вопросы (FAQ)

### Q: Как узнать, какие endpoints доступны в API?
A: Смотрите документацию API в `data/input/liloo_API v 2.0-120126-144149.pdf`

### Q: Что делать, если API возвращает ошибку?
A: Проверьте:
1. Правильность API ключей в `.env`
2. Доступность API сервера
3. Формат запроса (см. документацию API)

### Q: Как добавить логирование?
A: Используйте встроенный logger:
```python
from lilu_connector.utils.logger import get_logger

logger = get_logger(__name__)
logger.info("Сообщение")
```

## 🔗 Связанные документы

- [Документация API LILU](data/input/liloo_API%20v%202.0-120126-144149.pdf)
- [WooCommerce Connector README](../README.md) - для понимания общих паттернов
- [Архитектурные принципы](../IMPROVEMENTS_PROPOSAL_RU.md)

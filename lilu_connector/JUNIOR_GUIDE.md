# 📚 Руководство для Junior разработчиков

## 🎯 Цель этого документа

Этот документ поможет Junior разработчикам понять архитектуру LILU Connector и научиться работать с кодом.

## 📖 Содержание

1. [Основные концепции](#основные-концепции)
2. [Структура проекта](#структура-проекта)
3. [Как работает код](#как-работает-код)
4. [Как добавить новый функционал](#как-добавить-новый-функционал)
5. [Частые ошибки и их решение](#частые-ошибки-и-их-решение)
6. [Полезные ресурсы](#полезные-ресурсы)

---

## 🧠 Основные концепции

### Что такое API?

**API (Application Programming Interface)** - это способ, которым разные программы общаются друг с другом.

**Простая аналогия:**
- Представьте ресторан: меню - это API
- Вы (клиент) делаете заказ (запрос)
- Повар (сервер) готовит блюдо (обрабатывает запрос)
- Официант приносит блюдо (ответ)

**В нашем случае:**
- LILU API - это "ресторан" (сервер)
- LILUConnector - это "клиент" (наша программа)
- Мы отправляем HTTP запросы (заказы)
- API возвращает данные (блюда)

### Что такое HTTP запросы?

**HTTP (HyperText Transfer Protocol)** - это протокол для передачи данных в интернете.

**Основные типы запросов:**
- **GET** - получить данные (например, список клиентов)
- **POST** - создать новый ресурс (например, создать клиента)
- **PUT** - обновить ресурс (например, обновить данные клиента)
- **DELETE** - удалить ресурс (например, удалить клиента)

**Пример:**
```python
# GET запрос - получить список клиентов
clients = connector.get_clients()

# POST запрос - создать нового клиента
new_client = connector.create_client({
    'name': 'John Doe',
    'email': 'john@example.com'
})
```

### Что такое модели данных?

**Модель данных** - это структурированное представление данных.

**Почему это важно?**
Вместо работы со словарями (dict), мы используем типизированные классы.

**Пример:**
```python
# Плохо (работа со словарями):
client = {'id': 1, 'name': 'John', 'email': 'john@example.com'}
print(client['name'])  # Опечатка в ключе не будет обнаружена

# Хорошо (работа с моделями):
client = ClientModel(id=1, name='John', email='john@example.com')
print(client.name)  # IDE подскажет правильное поле
```

---

## 📁 Структура проекта

### Обзор структуры

```
lilu_connector/
├── __init__.py              # Точка входа в пакет
├── connector.py             # Главный класс для работы с API
├── README.md                # Основная документация
├── JUNIOR_GUIDE.md          # Этот файл
│
├── api/                     # Слой работы с API
│   ├── __init__.py
│   ├── client.py           # HTTP клиент
│   └── exceptions.py       # Специальные ошибки
│
├── models/                  # Модели данных
│   ├── __init__.py
│   ├── client.py           # Модель клиента
│   ├── product.py          # Модель продукта
│   └── order.py            # Модель заказа
│
├── config/                  # Конфигурация
│   ├── __init__.py
│   ├── settings.py         # Настройки подключения
│   └── constants.py        # Константы (URL, endpoints)
│
├── utils/                   # Вспомогательные функции
│   ├── __init__.py
│   └── logger.py           # Логирование
│
└── templates/               # Шаблоны для разработки
    ├── api_template.py     # Шаблон для нового API endpoint
    └── model_template.py   # Шаблон для новой модели
```

### Описание каждой папки

#### `api/` - Слой работы с API
**Что здесь:** Код, который делает HTTP запросы к серверу.

**Для Junior:** Это "низкий уровень" - здесь мы работаем с HTTP напрямую.
Не нужно знать бизнес-логику, только отправлять запросы и получать ответы.

**Основные файлы:**
- `client.py` - HTTP клиент (делает GET, POST, PUT, DELETE запросы)
- `exceptions.py` - Специальные ошибки для API

#### `models/` - Модели данных
**Что здесь:** Структуры данных, которые представляют объекты из API.

**Для Junior:** Это "средний уровень" - здесь мы работаем с данными в удобном формате.

**Основные файлы:**
- `client.py` - Модель клиента
- `product.py` - Модель продукта
- `order.py` - Модель заказа

#### `config/` - Конфигурация
**Что здесь:** Настройки подключения и константы.

**Для Junior:** Здесь хранятся все настройки, которые можно изменить без изменения кода.

**Основные файлы:**
- `settings.py` - Настройки из .env файла
- `constants.py` - Константы (URL, endpoints, таймауты)

#### `utils/` - Вспомогательные функции
**Что здесь:** Общие функции, которые используются в разных местах.

**Для Junior:** Здесь хранятся "инструменты", которые помогают в работе.

**Основные файлы:**
- `logger.py` - Логирование (запись информации о работе программы)

#### `templates/` - Шаблоны
**Что здесь:** Шаблоны кода для создания новых функций.

**Для Junior:** Используйте эти шаблоны как основу для нового кода.

---

## 🔄 Как работает код

### Пример: Получение списка клиентов

Давайте разберём, что происходит, когда мы вызываем `connector.get_clients()`:

```python
connector = LILUConnector()
clients = connector.get_clients()
```

#### Шаг 1: Инициализация
```python
connector = LILUConnector()
```

**Что происходит:**
1. Читаются настройки из `.env` файла (`LILUSettings`)
2. Создаётся HTTP клиент (`LILUClient`)
3. Настраиваются заголовки и аутентификация

#### Шаг 2: Вызов метода
```python
clients = connector.get_clients()
```

**Что происходит внутри `get_clients()`:**
1. Формируется endpoint: `/api/v2/clients`
2. Добавляются параметры: `{'page': 1, 'limit': 50}`
3. Вызывается `self.client.get(endpoint, params=params)`
4. HTTP клиент делает GET запрос к API
5. Ответ преобразуется в список `ClientModel`
6. Возвращается список клиентов

#### Шаг 3: Обработка ошибок
Если что-то пошло не так:
- `AuthenticationError` - неверный API токен
- `NetworkError` - проблемы с сетью
- `NotFoundError` - ресурс не найден
- `LILUAPIError` - другие ошибки API

---

## 🛠️ Как добавить новый функционал

### Пример: Добавить метод для получения счетов (invoices)

#### Шаг 1: Добавить endpoint в константы

**Файл:** `config/constants.py`

```python
ENDPOINTS = {
    # ... существующие endpoints ...
    'invoices': '/api/v2/invoices',
    'invoice': '/api/v2/invoices/{invoice_id}',
}
```

#### Шаг 2: Создать модель

**Файл:** `models/invoice.py`

Используйте шаблон из `templates/model_template.py`:

```python
from dataclasses import dataclass
from typing import Optional, Dict, Any

@dataclass
class InvoiceModel:
    id: int
    client_id: int
    amount: float
    status: str
    created_at: Optional[str] = None
    
    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> 'InvoiceModel':
        return cls(
            id=data.get('id', 0),
            client_id=data.get('client_id', 0),
            amount=float(data.get('amount', 0.0)),
            status=data.get('status', 'pending'),
            created_at=data.get('created_at')
        )
    
    def to_dict(self) -> Dict[str, Any]:
        return {
            'id': self.id,
            'client_id': self.client_id,
            'amount': self.amount,
            'status': self.status,
        }
```

#### Шаг 3: Добавить метод в connector

**Файл:** `connector.py`

Используйте шаблон из `templates/api_template.py`:

```python
def get_invoices(self, client_id: Optional[int] = None) -> List[InvoiceModel]:
    """Получить список счетов."""
    try:
        endpoint = ENDPOINTS['invoices']
        params = {}
        
        if client_id:
            params['client_id'] = client_id
        
        response = self.client.get(endpoint, params=params)
        data = response.json()
        
        invoices = [InvoiceModel.from_dict(item) for item in data]
        logger.info(f"Retrieved {len(invoices)} invoices")
        return invoices
    
    except Exception as e:
        logger.error(f"Error while fetching invoices: {e}")
        raise LILUAPIError(f"Failed to fetch invoices: {e}")
```

#### Шаг 4: Добавить импорт модели

**Файл:** `models/__init__.py`

```python
from .invoice import InvoiceModel

__all__ = [
    # ... существующие модели ...
    "InvoiceModel",
]
```

#### Шаг 5: Использовать новый метод

```python
connector = LILUConnector()
invoices = connector.get_invoices(client_id=123)
```

---

## ❌ Частые ошибки и их решение

### Ошибка 1: `ConfigurationError: LILU_API_TOKEN is required`

**Причина:** Не настроен `.env` файл или отсутствует API токен.

**Решение:**
1. Создайте файл `.env` в корне проекта
2. Добавьте настройки:
```env
LILU_API_URL=https://api.servus-ululu.com
LILU_API_TOKEN=your_api_token_here
```

### Ошибка 2: `AuthenticationError: Authentication failed`

**Причина:** Неверный API токен.

**Решение:**
1. Проверьте правильность токена в `.env`
2. Убедитесь, что токен не содержит лишних пробелов
3. Проверьте, что токен активен в системе LILU
4. Убедитесь, что токен не истёк (если у токенов есть срок действия)

### Ошибка 3: `NetworkError: Connection timeout`

**Причина:** Проблемы с сетью или сервер недоступен.

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте, что URL API правильный
3. Попробуйте увеличить таймаут в `.env`:
```env
LILU_TIMEOUT=60
```

### Ошибка 4: `NotFoundError: Resource with ID 123 not found`

**Причина:** Ресурс с указанным ID не существует.

**Решение:**
1. Проверьте правильность ID
2. Убедитесь, что ресурс существует в системе LILU

### Ошибка 5: `AttributeError: 'dict' object has no attribute 'name'`

**Причина:** Работаете со словарём вместо модели.

**Решение:**
Используйте модели вместо словарей:
```python
# Плохо:
client = response.json()  # Это словарь
print(client['name'])

# Хорошо:
client = ClientModel.from_dict(response.json())  # Это модель
print(client.name)
```

---

## 📚 Полезные ресурсы

### Документация проекта
- [API_NOTES.md](API_NOTES.md) - Ключевые моменты из инструкции LILU API
  - Аутентификация
  - Структура ответов
  - Особенности работы с клиентами
  - Примеры использования

### Документация Python
- [Официальная документация Python](https://docs.python.org/3/)
- [Tutorial для начинающих](https://docs.python.org/3/tutorial/)

### HTTP и API
- [MDN: HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)
- [REST API Tutorial](https://restfulapi.net/)

### Python библиотеки
- [requests](https://requests.readthedocs.io/) - для HTTP запросов
- [dataclasses](https://docs.python.org/3/library/dataclasses.html) - для моделей данных

### Лучшие практики
- [PEP 8 - Style Guide](https://pep8.org/)
- [Clean Code Python](https://github.com/ryanhaining/cpp-cheat-sheet)

---

## 💡 Советы для Junior разработчиков

1. **Читайте код внимательно** - понимание существующего кода важнее написания нового
2. **Используйте IDE** - автодополнение и проверка типов помогут избежать ошибок
3. **Пишите тесты** - тесты помогают убедиться, что код работает правильно
4. **Логируйте всё** - логи помогают понять, что происходит в программе
5. **Не бойтесь спрашивать** - лучше спросить, чем сделать неправильно
6. **Изучайте ошибки** - каждая ошибка - это возможность чему-то научиться
7. **Используйте шаблоны** - они помогут поддерживать единый стиль кода

---

## ❓ Вопросы?

Если у вас есть вопросы:
1. Прочитайте документацию в `README.md`
2. Посмотрите примеры в `templates/`
3. Изучите существующий код
4. Спросите у более опытных разработчиков

**Помните:** Каждый разработчик когда-то был Junior. Не бойтесь задавать вопросы!

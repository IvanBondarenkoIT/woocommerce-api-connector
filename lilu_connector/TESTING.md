# 🧪 Тестирование LILU Connector

## 📋 Быстрый старт

### 1. Тестовое подключение

Проверьте, что подключение к API работает:

```bash
python scripts/test_lilu_connection.py
```

Этот скрипт проверит:
- ✅ Инициализацию коннектора
- ✅ Доступность API (health check)
- ✅ Получение списка клиентов
- ✅ Получение списка продуктов
- ✅ Получение списка заказов

### 2. Создание тестового клиента

Создайте тестового клиента в системе LILU:

```bash
python scripts/create_test_client.py
```

Этот скрипт:
- ✅ Создаст тестового клиента с уникальным именем и email
- ✅ Покажет информацию о созданном клиенте
- ✅ Вернёт ID клиента для дальнейшей работы

## 🔍 Что проверяют скрипты

### test_lilu_connection.py

1. **Инициализация** - проверяет, что настройки загружены правильно
2. **Health Check** - проверяет доступность API
3. **Получение данных** - тестирует основные методы API:
   - `get_clients()` - получение клиентов
   - `get_products()` - получение продуктов
   - `get_orders()` - получение заказов

### create_test_client.py

1. **Подключение** - проверяет подключение к API
2. **Создание клиента** - создаёт нового клиента с тестовыми данными
3. **Валидация** - проверяет, что клиент создан успешно

## ⚠️ Возможные ошибки

### ConfigurationError

**Симптомы:**
```
❌ ОШИБКА КОНФИГУРАЦИИ:
   LILU_API_TOKEN is required.
```

**Решение:**
1. Убедитесь, что файл `.env` существует в корне проекта
2. Проверьте, что в `.env` есть:
   ```env
   LILU_API_URL=https://api.servus-ululu.com
   LILU_API_TOKEN=your_token_here
   ```
3. Убедитесь, что значения не содержат лишних пробелов

### AuthenticationError

**Симптомы:**
```
❌ ОШИБКА АУТЕНТИФИКАЦИИ:
   Authentication failed with status 401
```

**Решение:**
1. Проверьте правильность `LILU_API_TOKEN` в `.env`
2. Убедитесь, что токен активен в личном кабинете LILU
3. Проверьте, что токен не истёк

### NetworkError

**Симптомы:**
```
❌ ОШИБКА СЕТИ:
   Connection timeout
```

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте правильность `LILU_API_URL`
3. Попробуйте увеличить таймаут в `.env`:
   ```env
   LILU_TIMEOUT=60
   ```

## 📝 Примеры использования

### Интерактивное тестирование

Вы можете также тестировать подключение в Python REPL:

```python
# Запустите Python
python

# Импортируйте коннектор
from lilu_connector import LILUConnector

# Создайте коннектор
connector = LILUConnector()

# Проверьте подключение
connector.health_check()

# Получите список клиентов
clients = connector.get_clients()
print(f"Найдено клиентов: {len(clients)}")

# Создайте тестового клиента
new_client = connector.create_client({
    'name': 'Тестовый клиент',
    'email': 'test@example.com',
    'phone': '+79991234567'
})
print(f"Создан клиент с ID: {new_client.id}")
```

## 🔧 Настройка для тестирования

### Рекомендуемые настройки в `.env`

```env
# Базовые настройки
LILU_API_URL=https://api.servus-ululu.com
LILU_API_TOKEN=your_token_here
LILU_API_VERSION=v2

# Настройки для тестирования (увеличенные таймауты)
LILU_TIMEOUT=60
LILU_MAX_RETRIES=5
LILU_RETRY_DELAY=2
```

## 📊 Интерпретация результатов

### Успешное подключение

Если всё работает правильно, вы увидите:

```
================================================================================
ТЕСТИРОВАНИЕ ПОДКЛЮЧЕНИЯ К LILU API
================================================================================

📋 Шаг 1: Инициализация коннектора...
✅ Коннектор успешно инициализирован
   URL: https://api.servus-ululu.com
   Версия API: v2

📋 Шаг 2: Проверка доступности API (health check)...
✅ API доступен и отвечает

📋 Шаг 3: Получение списка клиентов...
✅ Успешно получено 5 клиентов

================================================================================
✅ ТЕСТИРОВАНИЕ ЗАВЕРШЕНО УСПЕШНО
================================================================================
```

### Частичный успех

Если некоторые методы не работают, скрипт покажет предупреждения, но продолжит работу:

```
⚠️  Не удалось получить продукты: API Error 404: Endpoint not found
```

Это нормально, если некоторые endpoints ещё не реализованы в API.

## 🚀 Следующие шаги

После успешного тестирования:

1. **Изучите документацию API** в `data/input/liloo_API v 2.0-120126-144149.pdf`
2. **Добавьте нужные методы** используя шаблоны из `templates/`
3. **Напишите свои тесты** для специфичных случаев
4. **Интегрируйте** коннектор в ваше приложение

## 💡 Советы

1. **Запускайте тесты регулярно** - это поможет обнаружить проблемы раньше
2. **Проверяйте логи** - они содержат полезную информацию для отладки
3. **Используйте тестовые данные** - не создавайте реальных клиентов при тестировании
4. **Сохраняйте ID тестовых клиентов** - они могут понадобиться для дальнейших тестов

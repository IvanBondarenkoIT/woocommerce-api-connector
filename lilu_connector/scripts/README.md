# 📜 Скрипты для работы с LILU Connector

## 📋 Описание

Эта папка содержит утилитарные скрипты для тестирования и работы с LILU API.

## 🚀 Использование

Все скрипты можно запускать из корня проекта:

```bash
# Как Python модуль (рекомендуется)
python -m lilu_connector.scripts.test_connection
python -m lilu_connector.scripts.fetch_clients
python -m lilu_connector.scripts.create_client
python -m lilu_connector.scripts.test_template_categories
python -m lilu_connector.scripts.check_env
```

## 📁 Доступные скрипты

### 1. `check_env.py`
Проверяет настройки `.env` файла.

```bash
python -m lilu_connector.scripts.check_env
```

**Что делает:**
- Проверяет наличие обязательных переменных
- Проверяет, что нет заглушек
- Показывает опциональные переменные

### 2. `test_connection.py`
Тестирует подключение к LILU API.

```bash
python -m lilu_connector.scripts.test_connection
```

**Что проверяет:**
- Инициализацию коннектора
- Доступность API
- Получение категорий шаблонов
- Получение клиентов
- Получение продуктов

### 3. `test_template_categories.py`
Тестирует получение категорий шаблонов сообщений.

```bash
python -m lilu_connector.scripts.test_template_categories
```

**Что делает:**
- Получает список категорий шаблонов
- Показывает информацию о категориях
- Сохраняет данные в JSON файл

### 4. `fetch_clients.py`
Получает клиентов из API и сохраняет в файл.

```bash
# JSON (по умолчанию)
python -m lilu_connector.scripts.fetch_clients

# CSV
python -m lilu_connector.scripts.fetch_clients --format csv

# Excel
python -m lilu_connector.scripts.fetch_clients --format excel
```

**Что делает:**
- Получает всех клиентов с пагинацией
- Сохраняет в выбранном формате
- Показывает статистику

### 5. `create_client.py`
Создаёт тестового клиента в системе LILU.

```bash
python -m lilu_connector.scripts.create_client
```

**Что делает:**
- Создаёт клиента с уникальным именем и email
- Показывает информацию о созданном клиенте
- Возвращает ID клиента

### 6. `create_musketeers.py`
Создаёт 4 тестовых клиента-мушкетеров с вымышленными данными.

```bash
python -m lilu_connector.scripts.create_musketeers
```

**Что делает:**
- Создаёт 4 клиентов: Д'Артаньян, Атос, Портос, Арамис
- Заполняет вымышленными данными (email, телефон, теги)
- Показывает результаты создания всех клиентов

## 📂 Структура

```
lilu_connector/scripts/
├── __init__.py                    # Инициализация модуля
├── README.md                      # Этот файл
├── check_env.py                   # Проверка настроек
├── test_connection.py             # Тест подключения
├── test_template_categories.py   # Тест категорий шаблонов
├── fetch_clients.py               # Получение и сохранение клиентов
└── create_client.py               # Создание тестового клиента
```

## 💡 Для Junior разработчиков

Все скрипты содержат подробные комментарии и обработку ошибок. Изучите их, чтобы понять, как работать с LILU API.

## 🔧 Требования

- Python 3.7+
- Установленные зависимости из `requirements.txt`
- Настроенный `.env` файл в корне проекта

## 📚 Дополнительная информация

- Полная документация: `lilu_connector/README.md`
- Руководство по настройке: `lilu_connector/SETUP_GUIDE.md`
- Руководство для Junior: `lilu_connector/JUNIOR_GUIDE.md`
